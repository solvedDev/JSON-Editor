/**
 * SCRIPTS FOR HIGHLIGHTING FUNCTIONALITY
 * [!!!] NOT IN USE
 * 
 * Creator: @solvedDev
 * Project: JSON Editor
 */

class Highlighter {
	constructor(pConfig) {
		this.config = pConfig;
	}

	highlight(pText) {
		let hT;
		for(key in this.config) {
			//hT = 
		}
	}
}
